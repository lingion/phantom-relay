# Optional managed-installation hook. Leave both values unset to open the
# user's default browser and let the installed extension reuse the recorded
# target page opened by this wake path.
# bundle_id=com.google.Chrome
# wake_command=./browser_host_launcher.sh
