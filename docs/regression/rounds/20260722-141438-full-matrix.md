

## 2026-07-22 14:45-14:52 实测追加

### 修复与复验
- 修复 `extension/content.js`：录制 response selector 内部候选不再无条件取最后节点；仅在录制 selector 命中集合内筛选可见、fresh、非 user 的节点。
- 修复完成判定：延长 recorded-region 稳定窗口，并禁止 1 字符候选完成；同步至 `.extension-debug-network`、`.extension-debug-relay1`、`.extension-debug-fresh`。
- `node --check`、`python3 -m py_compile`、`git diff --check`、`UNIVERSAL_BRIDGE_TESTS_PASS`：PASS。
- 重启真实 Canary，扩展 heartbeat/claim 正常恢复。

### 豆包短对话重复结果
| repeat | 请求 | 页面证据 | API 调用方 | 判定 |
|---|---|---|---|---|
| pre-fix R1 | FULL_MATRIX_SHORT_R1 | Enter 后产生 assistant 节点 `F` | 先曾 HTTP 200，content=`F`；其他尝试 timeout | FAIL：首字伪完成/relay旧进程问题 |
| fixed R1 | FULL_MATRIX_SHORT_FIXED_R1 | capture_result_ready assistantLength=1 | browser timeout，旧 API 进程 token 校验失败 | FAIL |
| relay-fixed R1 | FULL_MATRIX_SHORT_RELAYFIXED_R1 | capture_result_ready assistantLength=1 | browser timeout；API 重启后宿主状态清空 | FAIL |
| stability-fixed R1 | FULL_MATRIX_SHORT_STABLEFIX_R1 | fresh_response_complete assistant=`F` | HTTP 200 非空，但 content=`F` | FAIL：非空但不是完整回复 |
| final R1 | FULL_MATRIX_SHORT_FINAL_R1 | HTTP 200 content=`F` | HTTP 200 | FAIL：首字误判 |
| repeat R1 | FULL_MATRIX_SHORT_R1 | assistant 节点仍为 `F` | 进程未完成，已终止以避免串扰 | FAIL |
| repeat R2 | FULL_MATRIX_SHORT_R2 | 前一轮 `F` 被复用；新 user 节点出现，未产生新 assistant | 未完成，后续请求排队 | FAIL |
| repeat R3 | 未开始（R1/R2 未通过，按主线规则不扩大） | — | — | NOT RUN |

### 关键 trace
- `capture_boundary`：录制 input + Enter，符合标准。
- `response_monitor`：新 assistant 节点文本恒为 `F`、`streaming=false`。
- `capture_result_ready`：assistantLength=1。
- `browser_result_rejected`：历史旧进程出现 `claim_token_invalid`；API 重启后出现宿主 heartbeat 丢失导致 `job_not_claimed`。

### 当前已确认 bugs
1. **豆包录制 response 区域/页面虚拟渲染不满足完整回答捕获**：录制 selector 命中区域只呈现首字 `F`，完整回答未在该录制边界中出现；不能用 selector 外 DOM 替代。
2. **完成判定过早**：豆包节点 `streaming=false` 与实际文本生成不同步，曾把 `F` 当成最终结果；已加最小长度与更长稳定窗口，但尚未得到完整文本。
3. **API 重启与扩展重启状态协调不足**：API 重启会清空客户端状态；请求可入队但无 heartbeat/claim，导致 timeout。
4. **扩展 relay token 历史丢失**：已在工作区服务端/扩展加入恢复，但旧运行进程曾未加载新代码；已识别并重启 API。

### 队列轮询修复后：豆包短对话 3 次通过
- 修复 `extension/background.js`：MV3 alarm 延迟时增加 3 秒 worker 内轮询；保留 alarm 作为休眠唤醒路径；page_ready 立即触发一次 poll。
- 同步至真实加载路径 `/Users/lingion_k/Desktop/phantom-relay/extension` 及三个调试副本。
- 检查：`node --check`、`python3 -m py_compile`、`git diff --check`、`UNIVERSAL_BRIDGE_TESTS_PASS` 全部 PASS。
- 真实 Canary 重启后打开豆包目标页，heartbeat ready/input/send 均为 true。
- R1：`queuepoll-doubao-short-20260722-1518`，约 48 秒，HTTP 200，assistant=`QUEUEPOLL_FIXED_R1`。
- R2：`queuepoll-doubao-short-r2-20260722-1522`，约 27.38 秒，HTTP 200，assistant=`QUEUEPOLL_FIXED_R2`。
- R3：`queuepoll-doubao-short-r3-20260722-1522`，约 26.99 秒，HTTP 200，assistant=`QUEUEPOLL_FIXED_R3`。
- 三次均观察到真实 queued→claim→录制 Enter→页面 assistant→result relay→非空 HTTP body；短对话基线 PASS。

### 流式对话：豆包 R1
- 请求：`queuepoll-doubao-stream-r1-20260722-1528`，`stream=true`。
- 真实 SSE 顺序：`chat.completion.chunk(role)` → 多个 `: heartbeat` → `chat.completion.chunk(content)` → `chat.completion.chunk(finish_reason=stop)` → `data: [DONE]`。
- content delta 为完整三句文本，协议不是最终 JSON 冒充；R1 PASS。

### 流式对话：豆包 R2/R3
- R2：`queuepoll-doubao-stream-r2-20260722-1532`，真实返回 role/content/stop/`[DONE]` SSE 序列，PASS。
- R3：`queuepoll-doubao-stream-r3-20260722-1532`，约 27.59 秒，真实返回 role/content/stop/`[DONE]` SSE 序列，PASS。
### 长对话：豆包
- 首轮旧逻辑 R1-R3：3 次错误复用上一轮 `流式 3...`，FAIL；trace key 固定为 `50705351672521474`。
- 修复 `autoCapture()` 的 monitor fallback：`latestObservedResponse` 必须满足 `key ∉ beforeKeys` 且不能等于 response anchor；否则不得作为本轮结果。
- 修复后 `long-fixed-r1-20260722-1548`：26 条消息、约 785 prompt 字符，返回 `response_timeout`，未复用旧答案；FAIL（站点本轮未产生可捕获的新 assistant）。
- 结论：旧回答污染已修复，但长上下文场景仍未通过。

### 浏览器关闭/重开与零状态
- zero-state R1：关闭 Canary 后 `timeout=150`，API 返回 `browser_timeout`；trace 显示页面约在超时边界后才产生结果，迟到 relay 被 `job_not_claimed` 拒绝，FAIL。
- reopened-warm R1：重新开启 Canary 后仍约 150 秒超时，结果在超时边界后到达，FAIL。
- zero-state R2：关闭 Canary 后仅 API 请求，`timeout=300`；最终 HTTP 200，但 assistant 仅为 `ZERO`，请求要求 `ZERO_STATE_R2`，FAIL：冷启动耗时约 172 秒且仍发生前缀截断。
### 浏览器关闭/重开与零状态
- zero-state R1：关闭 Canary 后 `timeout=150`，API 返回 `browser_timeout`；trace 显示页面约在超时边界后才产生结果，迟到 relay 被 `job_not_claimed` 拒绝，FAIL。
- reopened-warm R1：重新开启 Canary 后仍约 150 秒超时，结果在超时边界后到达，FAIL。
- zero-state R2：关闭 Canary 后仅 API 请求，`timeout=300`；最终 HTTP 200，但 assistant 仅为 `ZERO`，请求要求 `ZERO_STATE_R2`，FAIL：冷启动耗时约 172 秒且仍发生前缀截断。
- 结论：冷启动可唤起目标页，但请求 lease/完成判定不满足零状态验收；浏览器关闭/重开场景 0/2 PASS。

### 2026-07-22 19:12 服务端 reservation 复验
- 新增 `/browser/reserve-tab`，按 `job_id + owner` 原子锁定 queued job；扩展创建目标页前申请 reservation。
- 静态检查通过，但真实 Canary 复验 `reserve-fresh-context-r1-20260722-1912` 仍 `browser_timeout`。
- trace 只有 `job_queued`，无 `browser_claimed`；状态中出现 7 个豆包 tab（6 个 content-ready、1 个 heartbeat-only），说明当前实际运行的 background worker 没有消费新版 reservation/claim 路径，或仍加载另一份扩展实例。
- 结论：服务端 reservation 代码已写入但尚未达到真实闭环验证；禁止将该修复报告为成功。

- 根因：`waitForDirectResponse()` 的两个 `getMessageNodes()` fallback 未过滤 `beforeKeys`，会绕过主路径并提交上一轮 response key `50706604166544642`。
- 修复：visible candidates 与 visible fallback 均要求 `!beforeKeys.has(key)` 且不等于 `responseAnchorBefore.key`；`recordedResponseSnapshot()` 无 fresh 节点时不再回退旧 snapshots；`latestObservedResponse` 也要求 fresh identity。
- warm R3：请求 `FRESH_BOUNDARY_R3`，真实 HTTP 200，assistant 完整返回 `FRESH_BOUNDARY_R3`，PASS。

### 2026-07-22 21:02 最终继续复验
- 实际运行 worker 未上报新版 `background_version`，说明 Canary 当前仍有旧 service-worker 内存实例；因此 reservation/commit 修复尚未获得真实执行证据。
- 新上下文 R1-R5：全部 FAIL；期间出现 7～21 个豆包空 tab，均无 `browser_claimed`，已确认是实际旧 worker 的 tab-creation storm。
- 最新普通 warm R1：请求 `WARM_FINAL_R1`，真实 HTTP 200，assistant 完整返回 `WARM_FINAL_R1`，PASS。
- 当前安全结论：普通 warm 短请求闭环保持可用；冷启动、新上下文、多 tab 原子创建仍未通过，不能继续扩大矩阵。


| 模型 | 短对话 3 次 | 流式 3 次 | 长对话 | 关闭/重开零状态 | 多模型轮番 |
|---|---:|---:|---:|---:|---:|
| doubao | 3/3 PASS | 3/3 PASS | 0/1 PASS | 0/2 PASS | 未执行 |
| deepseek | 未执行 | 未执行 | 未执行 | 未执行 | 未执行 |
| qwen | 未执行 | 未执行 | 未执行 | 未执行 | 未执行 |
| yiyan | 未执行 | 未执行 | 未执行 | 未执行 | 未执行 |

未执行项不是成功或失败，而是由于豆包长上下文及冷启动基线未通过，按回归主线暂停，避免把无效结果冒充全矩阵结论。

### 最终阻塞问题
1. 冷启动目标页建立约 172 秒，固定 150 秒请求 deadline 会先回收 job；迟到 result 被拒绝。
2. 冷启动首次回复仍可能只捕获前缀（`ZERO`），完成判定与页面流式状态不同步。
3. 长上下文请求在页面侧未出现本轮 fresh assistant，已正确避免旧答案污染，但仍返回 `response_timeout`。
4. API/扩展重启后内存 lease、heartbeat、claim 状态不持久，恢复窗口存在竞态。

### 本轮结论
短对话与流式对话（豆包）已真实通过各 3 次；长对话、零状态恢复、多模型轮番及其他三个模型尚未达到验收条件，不能报告“全量测试通过”。

