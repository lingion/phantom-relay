# Prefix guard regression — 2026-07-22 21:08 CST

## Scope
真实 Canary + 工作区扩展 + 原有豆包录制模板；未重新录制，未使用 CDP 执行输入/点击，未手工 POST browser/result。

## Code fixes under test
- 允许豆包复用同一回复节点：同 key 但文本变化视为新回复；同 key+同文本仍视为旧回复。
- Enter 发送证据不再因存在录制按钮而跳过检查；确认新用户/新录制回复/新观察事件后停止 Enter，避免重复提交。
- Enter 单次等待窗口调整为 8s/8s/10s，覆盖历史成功 trace 中约 5–6s 的豆包响应延迟。
- 对“请只回复：TOKEN”的严格前缀（如 `P`、`ST`、`ENTER`）禁止完成，必须等待同一录制区域增长。
- 空 snapshot 防护，避免 `Cannot read properties of null (reading 'text')`。
- 旧回复 key+旧文本不得作为本轮发送证据。

## Real caller results
- R1: PASS — HTTP 200，assistant=`PREFIX_GUARD_FIX_R1`，长度 19。
- R2 initial: FAIL — 内部 null 异常，未计入；已修复并重跑。
- R2B: PASS — HTTP 200，assistant=`PREFIX_GUARD_FIX_R2B`，长度 20。
- R3 initial: FAIL — 旧 assistant 被错误当发送证据，已修复并重跑。
- R3B: FAIL — HTTP browser_timeout。真实 trace 显示只执行一次 Enter，新用户消息出现，assistant 仅为 `P`，120s 内未增长；无旧 key 污染，无 fallback 事件。

## Current acceptance count
修复版本完整真实返回：2 次 PASS；第三次仍需独立成功，不能按 3/3 报告。

## Required checks
`node --check extension/content.js` PASS
`node --check extension/background.js` PASS
`python3 -m py_compile server/api_server.py` PASS
`git diff --check` PASS
`node tests/test_universal_bridge.js` PASS (`UNIVERSAL_BRIDGE_TESTS_PASS`)
