# Round 000 — 24小时计划冻结

- 时间戳：2026-07-22T09:39:17+0800
- 证据等级：baseline
- 计划文件：`docs/REGRESSION_24H_PLAN.md`
- 主线文件：`docs/REGRESSION_MAINLINE.md`
- 录制规范：`docs/RECORDED_STANDARD.md`
- 目标：先恢复GitHub已知可用的豆包/DLM非流式，再逐步扩展真实E2E能力。

## 当前状态

- 未启动浏览器、API或测试任务。
- 未修改业务代码。
- 当前工作树相对 origin/main 有大量未提交改动。
- 用户明确反馈：GitHub版本的豆包/DLM非流式可用；近期流式/诊断改动可能破坏该能力。

## 本计划的硬门槛

- 首条真实非流式请求未通过前，不继续扩大功能范围。
- 真实调用方 HTTP 200 + 非空 assistant 才算E2E通过。
- 静态审计、模拟job、页面DOM文字、手工result不算最终证据。
- 每轮只改一个边界；每轮必须写时间戳归档。

## 计划审查结果

- 计划分为基线锁定、真实宿主、录制核对、豆包/DLM非流式、普适特征归纳、SSE、多轮、刷新、切换、最小并发、错误恢复和收口。
- 计划明确保护已知非流式能力，避免继续以流式改动破坏基础功能。
- 计划明确把DeepSeek/豆包/千问等作为站点特征来源，不把特例写成全局规则。
- 计划保留休息和低频窗口，不以无意义任务数量替代有效证据。

## 下一轮唯一动作

执行R000基线锁定，然后检查真实GUI扩展宿主是否可用；不先跑静态审计，不先改流式。
