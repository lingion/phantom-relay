# Round 000 — 基线与回归主线冻结

- 时间戳：2026-07-22T09:36:27+0800
- 阶段：Stage 0 / baseline
- 结果：unverified（不是产品成功证据）

## 项目目标

Phantom Relay 是一个普适的浏览器 AI relay：把用户自己的浏览器登录态和用户录制的 input/send/response 三要素，转换成 OpenAI-compatible API，供 Cherry Studio 等客户端使用。

## 已知产品基线

- GitHub `origin/main`：`37216e0`。
- 当前 HEAD：`4f2cbae`。
- 当前工作树相对 `origin/main` 有大量未提交改动，主要涉及扩展发送/捕获、API 队列/SSE/会话逻辑和测试文件。
- 用户明确反馈：上传到 GitHub 的版本已经可以完成豆包和 DLM 的非流式；近期流式/诊断改动可能破坏了这条路径。

## 当前录制配置

- 豆包：input 已有；send 当前记录为 Enter 对象。
- DeepSeek：input + CSS send button；response 当前为空。
- 千问：input + Enter；response 有记录。
- 文心：input + Enter；response 有记录。

以上只记录文件现状，不代表真实当前浏览器状态。

## 本轮没有做的事

- 没有启动浏览器。
- 没有启动 API。
- 没有发送请求。
- 没有跑静态审计。
- 没有修改业务代码。

## 风险判断

当前最大风险不是缺少更多抽象，而是工作树相对已知可用版本发生了大范围变化，尤其是流式/捕获/发送链可能影响豆包和 DLM 非流式。

## 下一轮唯一动作

在真实用户 GUI 扩展宿主可用后，先以 GitHub 已知可用的豆包/DLM 非流式短请求为回归基准；只记录调用方真实 HTTP 结果，不先扩展流式或其他网站。
