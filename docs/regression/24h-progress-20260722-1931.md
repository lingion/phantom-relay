# 24-hour regression progress snapshot — 2026-07-22 19:31 CST

## Acceptance basis
Only real Canary + real extension + recorded input/send/response + caller HTTP body counts.

## Completed against the plan

- R000/R001 baseline and worktree evidence recorded.
- R002 real host/API checks completed; API health is 200 and a real Doubao client is ready.
- R003 recording contracts inspected for all four configured routes.
- Doubao real queue/claim/send/relay path was exercised repeatedly.
- Historical caller-level evidence: Doubao warm short non-streaming 3/3 and warm streaming 3/3 passed in the earlier matrix archive.
- Current runtime worker/content version was verified as `2026-07-22.20`.
- Queue polling, stale-response isolation, prefix protection, completion-window protection, and result relay fixes were implemented and statically verified.
- Four site-feature records were created under `docs/regression/`.
- Every executed regression round has a timestamped archive under `docs/regression/rounds/`.

## Not completed

- Current Doubao recording probe failed: the recorded response region returned only `RE`; no current full-response acceptance run can be declared green until the recording is redone.
- Doubao long conversation: failed safely with `response_timeout` after stale-answer protection; no fresh assistant accepted.
- Browser closed/reopened zero-state: failed (`browser_timeout`/prefix result in earlier runs).
- DeepSeek: response recording is null; no valid current E2E run may start.
- Qwen: recording exists but no current live caller-level acceptance evidence.
- Wenxin/Yiyan: recording exists but no current live caller-level acceptance evidence.
- Model switching, multi-model same-context rotation, long/streaming/cold-start matrix for all models, Cherry Studio, and concurrency were not completed.

## Counted status

- Fully accepted current target scenarios: none under the latest recording probe.
- Historical Doubao warm short: 3/3 PASS.
- Historical Doubao warm streaming: 3/3 PASS.
- Doubao long: 0/1 PASS.
- Browser close/reopen zero-state: 0/2 PASS.
- DeepSeek/Qwen/Wenxin current full E2E: not run / not accepted.

## Current single blocker

The current Doubao recorded response selector `div.relative.grid.w-full` exposes only a partial prefix in the live page. The next valid action is to re-record the complete response region in the real GUI. Code-side selector broadening is prohibited by the recorded standard.

## Next execution order

1. Re-record Doubao complete response region through real GUI.
2. Verify the new recorded marker and run Doubao short 3x.
3. Run Doubao stream 3x and page-refresh short.
4. Re-test long and browser close/reopen.
5. Only after Doubao is green, proceed to DeepSeek after its missing response recording is supplied; then Qwen/Wenxin.
6. Run same-context multi-model rotation and final matrix only after each model has a valid basic caller-level path.
